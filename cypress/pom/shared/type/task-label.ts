/**
 * @description set of expected properties that exist on most buttons and
 * used for transitioning tasks via the main menu.
 */
export type taskLabels = {
  id: string
  label?: string
  labelRight?: string
}
