export enum SliderNames {
  OPACITY = 'Tool Opacity',
  ANGLE = 'Angle (deg)',
  WIDTH = 'Width (mm)',
  LENGTH = 'Length (mm)',
  OFFSET = 'Offset (mm)'
}

export enum OrientationOptions {
  TOWARDS = 'Towards',
  CENTER = 'Center',
  AWAY = 'Away'
}

export enum InstrumentDropdowns {
  CATEGORY = 'Category',
  TIP = 'Tip'
}
